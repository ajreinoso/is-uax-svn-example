#include <stdlib.h>
#include <stdio.h>
#include "f1.h"
int main(){
 printf("Let's start a new SE project...\n");
 int a=-1;
 positiveInt pi= myabs(a);
 printf("Valor abosluto de %d: %d\n", a, pi);
 exit(0);
} 
